id: code.service-with-di
suite: code-generation
difficulty: medium

fixture:
  path: fixtures/code.service-with-di

task:
  prompt: |
    `src/Container.php` is a tiny service container, and the verification test
    already wires `UserService` into it. The service itself is unfinished:
    implement `UserService::getName(int $id)` in `src/UserService.php` so it
    returns "Alice" for id 1, "Bob" for id 2, and "unknown" for any other id.

expected:
  pass_commands:
    - php tests/test.php
  expected_files_changed:
    - src/UserService.php
  forbidden_files_changed:
    - tests/test.php
