id: regular.scenario
suite: smoke
difficulty: easy

fixture:
  path: tests/Fixtures/runner-fixture-tree

task:
  prompt: Create the solution file.

expected:
  pass_commands:
    - 'php -r "exit(file_exists(''solution.txt'') ? 0 : 1);"'
